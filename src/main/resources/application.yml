server:
  port: ${PORT:8080}  # Valor por defecto si PORT no está configurado
  url: ${SERVER_URL:http://localhost:8080}  # Valor por defecto si SERVER_URL no está configurado

spring:
  datasource:
    url: jdbc:sqlserver://3.239.112.251:1433;databaseName=VentasDB;encrypt=false;trustServerCertificate=true;integratedSecurity=false;  ## Cambiar "model" por tu base de datos
    username: sa          ## Cambiar por tu usuario
    password: Diego141608!Sup3rSegura2026#  ## Cambiar por tu contraseña
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 20000
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update  # Puede ser 'create', 'update', 'validate' o 'none'
    show-sql: false  # Puede true o false, para ver las consultas SQL en consola

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html # Ruta para acceder a Swagger UI: http://localhost:8085/swagger-ui.html
